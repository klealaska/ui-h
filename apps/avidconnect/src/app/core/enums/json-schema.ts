export enum PropertyType {
  Complex = 'complex',
  Choice = 'choice',
  Text = 'text',
  Numeric = 'numeric',
  Boolean = 'boolean',
  Password = 'password',
  Auth = 'auth',
}

export enum ChoiceSelection {
  Single = 'single',
  Multiple = 'multiple',
  MultipleOrdered = 'multiple-ordered',
}
