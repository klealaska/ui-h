export enum PaymentStatus {
  exampleStatus = 'exampleStatus', // TODO: remove once all statuses are defined. Adding this for now to continue to show example status
  stopRequested = 'stopRequested',
}
