export enum ButtonHTMLTypes {
  button = 'button',
  submit = 'submit',
  reset = 'reset',
}

export enum ButtonTypes {
  primary = 'primary',
  secondary = 'secondary',
  tertiary = 'tertiary',
}

export enum ButtonColors {
  default = 'default',
  critical = 'critical',
  neutral = 'neutral',
}

export enum ButtonSizes {
  sm = 'sm',
  md = 'md',
  lg = 'lg',
}
