export enum TooltipStyles {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum TooltipPositions {
  Above = 'above',
  Below = 'below',
  Left = 'left',
  Right = 'right',
  Before = 'before',
  After = 'after',
}

export enum PointerPositions {
  Start = 'start',
  Center = 'center',
  End = 'end',
}
