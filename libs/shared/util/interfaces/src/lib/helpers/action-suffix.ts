export const pending = (action: string): string => `${action} Pending`;

export const success = (action: string): string => `${action} Success`;

export const failure = (action: string): string => `${action} Failure`;
