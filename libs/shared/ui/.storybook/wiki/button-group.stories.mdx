import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Wiki/Button-group" />

Button group is a group of buttons wrapped in a container to improve accessible. This can be done by creating a `div` and adding an `aria-label` attribute.
