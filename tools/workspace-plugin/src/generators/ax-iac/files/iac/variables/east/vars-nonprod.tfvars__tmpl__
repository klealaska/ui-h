is_production = false

capability = {
  name   = "<%= domain %>"
  abbrev = "<%= domain.replace(/-/g, "").substring(0,7) %>"
}

domain = {
  name   = "uicoe"
  abbrev = "uicoe"
}

keyVault_Domain = {
  name   = "User-Interface-Center-of-Excellence"
  abbrev = "uicoe"
}

location = "eastus"
